{
  "name": "@mentelm/mmcp-server-common",
  "version": "0.0.1",
  "type": "module",
  "dependencies": {
    "@mentelm/mmcp-slice-model": "workspace:*",
    "@modelcontextprotocol/server": "^2.0.0"
  },
  "main": "./dist/index.js",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo \"no tests for servers/common\"",
    "check-version": "bash ../../scripts/check-version.sh",
    "publish": "yarn npm publish --access public --staged"
  }
}
